# 三层地图视觉升级 V2

本版在原项目结构和交互逻辑上直接优化，不重写业务架构。

## 主要改动

- 三层独立语义配色
  - 基础设施网：琥珀金 `#FFB547`
  - 物流运营网：亮蓝 `#35B9FF`
  - 数字物流网：翠绿 `#46E69A`
- 中国省级地图挤出厚度由 0.72 提升至 1.08，并增强倒角、材质高光和自发光。
- 每层省界使用对应网络的高亮边界色，炸开后可直接依颜色识别网络层。
- 三层炸开距离扩大，并提高 Z 轴高度差；镜头改为更强的俯视透视，增强实体悬浮感。
- 增加多色环境灯、底部扫描环、星点背景和更亮的科技蓝空间环境。
- 炸开状态增加三网快捷标签以及少量垂直联动线，用于表达“三网同位/垂直穿透”。
- 节点增加更明显的自发光和双环效果；运营货流和数字关系线提高亮度与线宽。
- UI 从偏暗绿色主题调整为深蓝科技底色，并让三层颜色贯穿按钮、抽屉和图例。
- 保留原有：三网合一、三层分解、单层分析、实体穿透、任务追踪、搜索、图层开关等逻辑。

## 运行

Windows 环境直接运行：

```bash
npm run dev
```

如果需要重新安装依赖：

```bash
npm install
npm run dev
```

> 当前压缩包来源中自带的是 Windows 平台 node_modules，因此在 Linux 环境不能直接复用其中的 Rollup 原生二进制进行 Vite 构建；源码语法与现有测试已校验通过。


## V2.1 大间距炸开调整

- 三层分解中心间距由约 17.5 提升到 54 个场景单位，显著减少三张中国地图互相遮挡。
- 分解态地图比例调整为 0.78，在保证地图主体足够大的同时给层间留出完整可视空间。
- 分解镜头后退至 Z=270，并将 OrbitControls 最大距离提升到 320，保证上下层不被视窗裁切。
- 根节点俯视倾角由 16° 调整到 12°，兼顾实体厚度与完整轮廓。
- 三层垂直穿透线与标签位置同步跟随新的炸开高度。
