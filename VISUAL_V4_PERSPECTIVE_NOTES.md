# V4 立体视角优化

- 三层炸开整体倾角：14° -> 8°
- 炸开相机：`(0,-10,190)` -> `(0,-6,184)`，画面更接近正视
- 三层地图板 Z 厚度：`scale.z = 2.15`，并下移 `-1.24` 保持顶面高度不变
- 三层间距保持上一版 `-30 / 0 / +30`，避免重新出现大面积遮挡
- 目标：轮廓更完整、地图不再过斜，同时通过实体侧壁厚度强化 3D 感
