{
  "name": "national-logistics-network",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/build-worker.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "prepare:infrastructure": "python scripts/prepare-infrastructure-data.py",
    "extract:reference": "node scripts/extract-reference.mjs",
    "test": "node --test --test-isolation=none"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.1"
  }
}
