# V5 同轴垂直三层

- 三层地图的平面坐标保持完全一致，不再通过 Y 方向大幅错开来制造“炸开”效果。
- 三层统一缩放为 `0.94`，仅沿 Z 轴按 `0 / 13 / 26` 分层，保证省域、节点和线路能垂直对应。
- 炸开相机改为斜向观察同轴堆栈，以真实高度、地图实体侧壁和同位连接线表达立体关系。
- 场景采用 Z 轴向上，与地图的实际建模方向一致。
- 鼠标俯仰范围由约 `65°–115°` 改为 `4°–84°`，可从近俯视连续推到接近水平观察，同时禁止穿到地图底部。
- 三层标签收拢至画面中部，与更紧凑的垂直堆栈构图对应。
